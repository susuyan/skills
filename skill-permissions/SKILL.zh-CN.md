---
name: skill-permissions
description: 技能权限分析、一次性授权、skill权限管理、分析skill所需权限、批量授权
---

# Skill Permissions

自动分析 Claude Code 中所有技能所需的权限，并生成一次性授权命令。

## ⚠️ 危险操作警告

**本 skill 具有修改其他 skill 文件的能力。** 以下命令可能造成大范围文件变动：

| 命令 | 风险等级 | 说明 |
|------|---------|------|
| `/skill-permissions inject` | 🔴 高 | 批量修改所有 skill 的 SKILL.md |
| `/skill-permissions allow-all` | 🟡 中 | 批量修改 settings.json |
| `/skill-permissions allow <name>` | 🟢 低 | 仅修改 settings.json |
| `/skill-permissions <name>` | ✅ 安全 | 只读分析，不修改文件 |

**推荐做法：**
- 优先使用 **显式命令**（如 `/skill-permissions allow port-allocator`）
- 避免直接使用 `inject` 或 `allow-all`，除非你清楚了解其影响
- 批量操作前先运行 `/skill-permissions` 查看分析结果

## 使用方法

| 命令 | 说明 |
|------|------|
| `/skill-permissions` | 分析所有 skill 并显示权限摘要 |
| `/skill-permissions <skill-name>` | 分析指定 skill 的权限需求 |
| `/skill-permissions allow <skill-name>` | 为指定 skill 执行一次性授权 |
| `/skill-permissions allow-all` | ⚠️ 为所有 skill 执行一次性授权 |
| `/skill-permissions inject` | 🔴 为所有 skill 注入 allow 命令 |
| `/skill-permissions allow` | ⚠️ 为本 skill 执行一次性授权 |

## 工作原理

### 1. 权限分析

扫描 skill 的 SKILL.md 文件，识别以下模式：

**Bash 命令模式：**
```
find * → Bash(find *)
ls * → Bash(ls *)
cat ~/.claude/* → Bash(cat ~/.claude/*)
lsof -i:3* → Bash(lsof -i:3*)
git * → Bash(git *)
npm * → Bash(npm *)
```

**识别规则：**
- 代码块中的 bash 命令 (```bash ... ```)
- 行内命令 (`command`)
- 明确提到的系统命令

### 2. 权限规则生成

将识别到的命令转换为 `permissions.allow` 规则：

```json
{
  "permissions": {
    "allow": [
      "Bash(find * -name package.json *)",
      "Bash(ls -d *)",
      "Bash(cat ~/.claude/*)",
      "Bash(lsof -i:3*)"
    ]
  }
}
```

### 3. 一次性授权命令注入

为每个 skill 添加 `allow` 子命令：

```markdown
### 命令: `/<skill-name> allow`

执行一次性授权，将本 skill 所需的权限添加到 Claude Code 配置中。
```

## 执行步骤

### 命令: `/skill-permissions`

分析所有 skill 并显示权限摘要：

1. **扫描所有 skill**
   ```bash
   find ~/.claude/skills -name "SKILL.md" -type f 2>/dev/null
   ```

2. **对每个 skill 进行权限分析**
   - 读取 SKILL.md 内容
   - 提取 bash 代码块
   - 识别命令模式
   - 生成权限规则

3. **输出分析结果**

**权限识别正则：**
```
# 识别 bash 代码块
```bash\n([\s\S]*?)```

# 识别常见命令
(find|ls|cat|mkdir|mv|rm|cp|git|npm|yarn|pnpm|lsof|kill|curl|wget)\s+[^\n]+

# 识别路径模式
~/[^\s]+
~/.claude/[^\s]+
```

### 命令: `/skill-permissions <skill-name>`

分析指定 skill 的权限需求：

1. **定位 skill**
   ```bash
   SKILL_PATH=$(find ~/.claude/skills -type d -name "<skill-name>" 2>/dev/null | head -1)
   ```

2. **读取并分析 SKILL.md**

3. **输出详细权限列表**

### 命令: `/skill-permissions allow <skill-name>`

为指定 skill 执行一次性授权：

1. **分析 skill 权限需求**

2. **读取现有配置**
   ```bash
   cat ~/.claude/settings.json 2>/dev/null || echo '{"permissions":{"allow":[]}}'
   ```

3. **合并权限规则**（去重，保留现有规则）

4. **写入配置文件**

5. **输出授权结果**

### 命令: `/skill-permissions allow-all`

为所有 skill 执行一次性授权：

1. **扫描所有 skill**
2. **合并所有权限需求**
3. **去重并写入配置**
4. **输出汇总结果**

### 命令: `/skill-permissions inject`

为所有 skill 注入 `allow` 子命令：

1. **扫描所有 skill**

2. **检查是否已有 allow 命令**
   - 如果 SKILL.md 中已包含 `### 命令: \`/<skill-name> allow\``，跳过

3. **生成 allow 命令文档**
   ```markdown
   ### 命令: `/<skill-name> allow`

   执行一次性授权，将本 skill 所需的权限添加到 Claude Code 配置中：

   1. 读取 `~/.claude/settings.json`
   2. 合并以下权限到 `permissions.allow`：

   ```json
   {
     "permissions": {
       "allow": [
         <分析得到的权限列表>
       ]
     }
   }
   ```

   3. 写入配置文件
   4. 输出授权结果
   ```

4. **追加到 SKILL.md**（在 `## 执行步骤` 之后）

5. **输出注入结果**

## 权限模式映射表

| 命令模式 | 权限规则 |
|---------|---------|
| `find <path> -name <pattern>` | `Bash(find * -name * *)` |
| `ls -la <path>` | `Bash(ls *)` |
| `cat ~/.claude/*` | `Bash(cat ~/.claude/*)` |
| `mkdir -p <path>` | `Bash(mkdir *)` |
| `mv <src> <dst>` | `Bash(mv *)` |
| `ln -s <src> <dst>` | `Bash(ln *)` |
| `git init/add/commit` | `Bash(git *)` |
| `npm install/run` | `Bash(npm *)` |
| `lsof -i:<port>` | `Bash(lsof -i:*)` |

## 🚫 危险操作黑名单

以下命令模式会被**自动屏蔽**，不会添加到授权白名单中：

### 文件系统危险操作
| 命令 | 原因 |
|------|------|
| `rm *` | 删除文件，可能造成数据丢失 |
| `rm -rf *` | 递归强制删除，极其危险 |
| `rm -r *` | 递归删除 |
| `rmdir *` | 删除目录 |
| `> /dev/*` | 直接写入设备 |
| `dd *` | 低级磁盘操作 |
| `mkfs *` | 格式化文件系统 |
| `shred *` | 安全删除（不可恢复） |

### 权限与系统操作
| 命令 | 原因 |
|------|------|
| `sudo *` | 超级用户权限 |
| `su *` | 切换用户 |
| `chmod 777 *` | 开放所有权限 |
| `chmod -R *` | 递归修改权限 |
| `chown *` | 修改文件所有者 |
| `chgrp *` | 修改文件组 |

### 进程与网络危险操作
| 命令 | 原因 |
|------|------|
| `kill -9 *` | 强制杀死进程 |
| `killall *` | 杀死所有匹配进程 |
| `pkill *` | 按名称杀死进程 |
| `curl * \| bash` | 远程代码执行 |
| `wget * \| bash` | 远程代码执行 |
| `curl * \| sh` | 远程代码执行 |
| `eval *` | 动态执行代码 |
| `exec *` | 替换当前进程 |

### Git 危险操作
| 命令 | 原因 |
|------|------|
| `git push --force *` | 强制推送，可能覆盖远端 |
| `git push -f *` | 强制推送 |
| `git reset --hard *` | 硬重置，丢失未提交更改 |
| `git clean -fd *` | 删除未跟踪文件 |
| `git checkout -- *` | 丢弃本地更改 |

### 其他危险操作
| 命令 | 原因 |
|------|------|
| `:(){ :\|:& };:` | Fork 炸弹 |
| `> ~/.bashrc` | 覆盖 shell 配置 |
| `> ~/.zshrc` | 覆盖 shell 配置 |
| `crontab -r` | 删除所有定时任务 |
| `history -c` | 清除命令历史 |

## 黑名单处理逻辑

当分析 skill 时，如果检测到黑名单中的命令：

1. **不添加到授权列表** - 跳过该命令的权限规则生成
2. **输出警告** - 在分析结果中标记为 `🚫 已屏蔽`
3. **记录原因** - 说明为何该命令被屏蔽

**示例输出：**
```
📦 Skill: dangerous-skill
📁 位置: ~/.claude/skills/dangerous-skill

🔍 检测到的命令:
  ✅ find ~/Codes -name "*.js"
  ✅ cat ~/.claude/config.json
  🚫 rm -rf /tmp/cache (已屏蔽: 递归强制删除)
  🚫 sudo apt install (已屏蔽: 超级用户权限)
  🚫 kill -9 $PID (已屏蔽: 强制杀死进程)

📋 生成的权限规则 (仅安全命令):
  - Bash(find ~/Codes *)
  - Bash(cat ~/.claude/*)

⚠️ 3 个危险命令已被屏蔽，不会添加到授权列表
```

## 自定义黑名单

用户可以在 `~/.claude/skill-permissions-config.json` 中自定义黑名单：

```json
{
  "blocklist": {
    "patterns": [
      "rm *",
      "sudo *",
      "custom-dangerous-command *"
    ],
    "allow_override": [
      "kill -9 *"
    ]
  }
}
```

- `patterns`: 额外添加到黑名单的命令模式
- `allow_override`: 从默认黑名单中移除的命令（谨慎使用）

## 输出格式

### 分析结果（单个 skill）
```
📦 Skill: port-allocator
📁 位置: ~/.claude/skills/port-allocator

🔍 检测到的命令:
  - find ~/Codes -maxdepth 3 -name "package.json"
  - ls -d */
  - cat ~/.claude/port-registry.json
  - lsof -i:3000
  - lsof -ti:3000

📋 生成的权限规则:
  - Bash(find * -maxdepth * -name package.json *)
  - Bash(ls -d *)
  - Bash(cat ~/.claude/*)
  - Bash(lsof -i:3*)
  - Bash(lsof -ti:3*)

💡 运行 `/skill-permissions allow port-allocator` 执行授权
```

### 分析结果（所有 skill）
```
📊 Skill 权限分析报告

┌─────────────────┬────────────────────────────────────┬───────┐
│ Skill           │ 主要权限                            │ 状态   │
├─────────────────┼────────────────────────────────────┼───────┤
│ port-allocator  │ find, ls, cat, lsof                │ ⚠️ 未授权 │
│ share-skill     │ find, mv, ln, git                  │ ⚠️ 未授权 │
│ art-master      │ (无需特殊权限)                      │ ✅ 就绪  │
└─────────────────┴────────────────────────────────────┴───────┘

💡 运行 `/skill-permissions allow-all` 一次性授权所有 skill
```

### 授权成功
```
✅ 权限授权成功

📦 Skill: port-allocator

新增权限规则:
  - Bash(find * -maxdepth * -name package.json *)
  - Bash(ls -d *)
  - Bash(cat ~/.claude/*)
  - Bash(lsof -i:3*)
  - Bash(lsof -ti:3*)

配置文件: ~/.claude/settings.json

⚠️ 新权限将在下次会话生效，或运行 /clear 立即生效
```

### 注入成功
```
✅ Allow 命令注入完成

已更新的 skill:
  ✓ port-allocator - 添加了 `/port-allocator allow`
  ✓ share-skill - 添加了 `/share-skill allow`
  ⏭️ art-master - 已存在，跳过

💡 用户现在可以运行 `/<skill-name> allow` 进行一次性授权
```

## 配置文件

- **权限配置**: `~/.claude/settings.json`
- **skill 目录**: `~/.claude/skills/`

## Skill 首次调用集成

建议在 skill 的 SKILL.md 开头添加提示：

```markdown
## 首次使用

如果遇到权限提示，请先运行：
```
/<skill-name> allow
```

或者运行全局授权：
```
/skill-permissions allow-all
```
```

## 首次使用

如果遇到权限提示，请先运行：
```
/skill-permissions allow
```

### 命令: `/skill-permissions allow`

⚠️ **危险操作提示**

本 skill 能够自动分析并修改其他 skill 的文件。在执行授权前，请确认你了解以下风险：

1. **文件修改风险** - `inject` 命令会修改所有 skill 的 SKILL.md 文件
2. **权限扩大风险** - `allow-all` 会为所有 skill 添加权限，可能授予过多权限
3. **不可预见变动** - 批量操作可能影响你未曾查看的 skill

**推荐做法：** 优先使用显式命令，如 `/skill-permissions allow port-allocator`

---

执行一次性授权，将本 skill 所需的权限添加到 Claude Code 配置中：

1. 读取 `~/.claude/settings.json`
2. 合并以下权限到 `permissions.allow`：

```json
{
  "permissions": {
    "allow": [
      "Bash(find ~/.claude/skills *)",
      "Bash(cat ~/.claude/*)"
    ]
  }
}
```

3. 写入配置文件（保留现有权限）
4. 输出授权结果

**输出格式：**
```
⚠️ 危险操作提示

本 skill 具有修改其他 skill 文件的能力。
建议优先使用显式命令，如:
  /skill-permissions allow port-allocator
  /skill-permissions share-skill

确认继续授权？

---

✅ 已配置 Claude Code 权限

新增允许的命令模式：
  - Bash(find ~/.claude/skills *)
  - Bash(cat ~/.claude/*)

配置文件: ~/.claude/settings.json
```

## 注意事项

1. **保守分析** - 只识别明确出现的命令，避免过度授权
2. **去重合并** - 新权限与现有权限合并，不重复添加
3. **不删除** - 只添加权限，不会删除用户已有的权限配置
4. **通配符** - 使用通配符 `*` 匹配变化的参数部分
5. **会话生效** - 权限更新后需要新会话或 /clear 才能生效
6. **显式优先** - 推荐使用显式命令而非批量操作，减少意外风险
